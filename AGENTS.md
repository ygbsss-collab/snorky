# SNORKY 프로젝트 작업 지침

## 적용 범위

- 이 지침은 SNORKY 프로젝트의 모든 작업 요청에 적용한다.
- 사용자가 지정한 요청 범위와 파일 범위를 우선한다.
- 기존 source, API, Supabase, migration, SQL, specification을 요청 범위 밖에서 수정하지 않는다.
- 정책과 코드 또는 다른 문서가 충돌하면 기준을 임의로 수정하거나 확정하지 말고 사용자에게 보고한다.

## 실행 원칙

- 작업에 사용하는 토큰과 출력은 필요한 수준으로 최소화한다.
- 요청 해결에 필요한 파일만 확인한다.
- 동일 목적의 검색을 중복 수행하지 않는다.
- 요청 범위와 무관하거나 불필요한 테스트를 실행하지 않는다.
- 사용자가 별도로 요청하지 않으면 브라우저 또는 화면 검증을 수행하지 않는다.
- 변경 작업은 지정된 파일과 범위 안에서만 수행한다.
- 실제 변경 파일, diff, 검증 결과를 확인한 뒤 완료 여부를 판단한다.
- 프로젝트 전체를 선제적으로 탐색하거나 인덱싱하지 않는다.
- 사용자가 특정 파일 또는 기능을 지정하면 해당 파일과 직접 의존하는 최소 범위부터 확인한다.
- 원인 파악에 필요하지 않은 디렉터리와 파일은 열지 않는다.
- 이미 확인한 파일을 특별한 이유 없이 반복해서 읽지 않는다.
- 파일 전체 재작성보다 필요한 부분만 최소 수정한다.
- 요청하지 않은 리팩터링, 코드 정리, 구조 변경을 하지 않는다.
- 서브에이전트는 사용자가 명시적으로 요청하거나 단일 에이전트로 해결하기 어려운 경우에만 사용한다.
- 검증은 변경한 기능과 직접 관련된 최소 범위에서 한 번에 수행한다.
- 전체 빌드, 전체 테스트, 브라우저 검증은 사용자가 요청하거나 최소 검증으로 안전성을 판단할 수 없는 경우에만 수행한다.
- 동일한 검색, 테스트, 빌드 명령을 이유 없이 반복 실행하지 않는다.
- 대용량 생성 폴더(node_modules, dist, build, .git)는 요청 해결에 직접 필요한 경우가 아니면 탐색하지 않는다.
- 작업 중 장문의 진행 설명을 출력하지 않는다.
- 작업 범위 확대가 필요하면 먼저 이유와 추가로 확인할 파일을 사용자에게 보고하고 승인을 받은 뒤 진행한다.

## 보고 원칙

- 모든 진행 보고와 최종 보고는 한국어로 작성한다. 코드, 파일명, 명령어, identifier, 원문 오류 메시지는 원문을 유지할 수 있다.
- 완료 보고는 다음 형식을 사용한다.

  **변경 파일 / 핵심 변경 / 검증 결과 / [확인 필요]**

## 역할 분담 기준

### 사용자 역할
- 화면 검수, 기능 우선순위, 최종 판단은 사용자가 한다.
- UI 위치, 문구, 흐름, 사용성 판단은 사용자의 검수 결과를 기준으로 한다.
- 사용자가 제공한 스크린샷과 증상을 최우선 근거로 삼는다.

### ChatGPT 역할
- 사용자의 증상과 스크린샷을 기준으로 원인을 정리한다.
- Codex가 수행할 최소 수정 범위와 명령문을 작성한다.
- 수정 전 확인이 필요한 경우, 먼저 IDE에서 확인할 항목을 짧게 제시한다.
- 직접 파일 수정자처럼 행동하지 않고, 작업 범위와 순서를 조율한다.
- Codex에 넘길 명령은 필요한 파일, 수정 범위, 금지사항, 검증 방식만 포함해 짧게 작성한다.

### Codex CLI 역할
- 실제 코드와 SQL 파일을 수정한다.
- 지정된 파일과 직접 관련된 최소 범위만 확인한다.
- 전체 프로젝트 탐색, 전체 리팩터링, 전체 파일 재작성은 하지 않는다.
- 브라우저 화면 검수는 하지 않는다.
- 검증은 문법, 함수명 연결, diff 확인 등 최소 범위로 한다.
- 완료 보고는 변경 파일 / 핵심 변경 / 검증 결과 / [확인 필요] 형식으로만 한다.

### IDE / Antigravity 역할
- 파일 구조, diff, 검색 결과를 사용자가 확인하는 도구로 사용한다.
- 화면 검수는 사용자가 직접 한다.
- Codex 수정 전후 파일 변경 위치와 함수명 불일치 여부를 확인하는 보조 도구로 사용한다.
- IDE Agent가 Codex와 같은 파일을 동시에 수정하지 않는다.
- IDE Agent는 사용자가 명시적으로 요청한 경우에만 수정 작업을 수행한다.

### 작업 순서 기준
1. 사용자가 증상, 목표, 스크린샷을 제공한다.
2. ChatGPT가 원인과 최소 확인 범위를 정리한다.
3. 필요하면 사용자가 IDE에서 파일명, 함수명, diff만 먼저 확인한다.
4. ChatGPT가 Codex용 최소 명령을 작성한다.
5. Codex CLI가 실제 수정한다.
6. 사용자가 화면을 검수한다.
7. 화면 문제가 있으면 스크린샷과 증상 기준으로 다음 수정 범위를 다시 좁힌다.

### 금지 기준
- Codex에게 화면 검수를 맡기지 않는다.
- ChatGPT가 Codex 역할처럼 무조건 수정 명령부터 내리지 않는다.
- IDE Agent와 Codex CLI를 같은 파일에 동시에 투입하지 않는다.
- 원인 확인 없이 전체 프로젝트를 탐색하지 않는다.
- UI 판단을 코드 작성자가 임의로 확정하지 않는다.
- 사용자가 검수하지 않은 화면 결과를 완료로 단정하지 않는다.
