# SNORKY 프로젝트 작업 지침 (Codex용)

> **이 파일은 Codex 전용이다.** Claude Code는 이 파일이 아니라 `CLAUDE.md`를 역할 기준으로 따른다. Claude Code가 이 파일을 읽더라도 "구현 담당"으로 행동하지 않는다.

## 적용 범위
- 이 지침은 SNORKY 프로젝트의 모든 작업 요청에 적용한다.
- 사용자가 지정한 요청 범위와 파일 범위를 우선한다.
- 기존 source, API, Supabase, migration, SQL, specification을 요청 범위 밖에서 수정하지 않는다.
- 정책과 코드 또는 다른 문서가 충돌하면 기준을 임의로 수정하거나 확정하지 말고 사용자에게 보고한다.

## 실행 원칙
- 판단 기준은 **토큰 최적화(가성비)** 와 **개발 속도 향상**이다.
- 요청 해결에 필요한 파일만 확인한다. 프로젝트 전체를 선제적으로 탐색하거나 인덱싱하지 않는다.
- 사용자가 특정 파일/기능을 지정하면 해당 파일과 직접 의존하는 최소 범위부터 확인한다.
- 이미 확인한 파일을 반복해서 읽지 않고, 동일 목적의 검색·테스트·빌드를 반복 실행하지 않는다.
- 파일 전체 재작성보다 필요한 부분만 최소 수정한다.
- 요청하지 않은 리팩터링, 코드 정리, 구조 변경을 하지 않는다.
- 대용량/생성 폴더(node_modules, dist, build, .git)와 backup_*, backups, scratch, .chrome-* 폴더는 요청 해결에 직접 필요한 경우가 아니면 열지 않는다.
- 서브에이전트는 사용자가 요청하거나 단일 에이전트로 해결하기 어려운 경우에만 사용한다.
- 검증은 변경한 기능과 직접 관련된 최소 범위에서 한 번에 수행한다. (문법, 함수명 연결, git diff)
- 전체 빌드·전체 테스트·브라우저/화면 검증은 사용자가 요청했거나 최소 검증으로 안전성을 판단할 수 없을 때만 수행한다.
- 작업 중 장문의 진행 설명을 출력하지 않는다.
- 작업 범위 확대가 필요하면 먼저 이유와 추가로 확인할 파일을 보고하고 승인을 받은 뒤 진행한다.
- 실제 변경 파일, diff, 검증 결과를 확인한 뒤 완료 여부를 판단한다.

## Claude Code 계획서를 받았을 때
- 계획서 첫 줄이 "아래 분석을 가설로 취급하고 현재 레포에서 먼저 검증한다."이다. 계획서의 파일명·함수명·원인을 **현재 레포와 먼저 대조**한 뒤 구현한다.
- 계획서의 **[확인 필요]** 항목은 임의로 확정하지 말고 확인 결과를 보고한다.
- 계획서의 "수정하지 말 것" 범위는 지킨다. 계획과 실제 코드가 다르면 멈추고 보고한다.

## 보고 원칙
- 모든 진행 보고와 최종 보고는 한국어로 작성한다. 코드, 파일명, 명령어, identifier, 원문 오류 메시지는 원문을 유지할 수 있다.
- 완료 보고 형식:

  **변경 파일 / 핵심 변경 / 검증 결과 / [확인 필요]**

## 역할 분담 기준

### 사용자
- 화면 검수, 기능 우선순위, 최종 판단은 사용자가 한다.
- UI 위치·문구·흐름·사용성 판단은 사용자의 검수 결과를 기준으로 한다.
- 사용자가 제공한 스크린샷과 증상을 최우선 근거로 삼는다.

### Claude Code (분석·계획)
- 원인 분석, 영향 범위, 최소 수정 범위, Codex용 계획서를 작성한다.
- 기본적으로 파일을 직접 수정하지 않는다.
- Codex 수정 결과의 2차 리뷰와 회귀 위험 분석을 한다.

### ChatGPT
- 일반 업무와 SNORKY 상위 요구사항 정리. 이 레포의 코드 수정 지시자가 아니다.

### Codex (구현) — 이 파일의 대상
- 실제 코드와 SQL 파일을 수정한다.
- 지정된 파일과 직접 관련된 최소 범위만 확인한다.
- 전체 프로젝트 탐색, 전체 리팩터링, 전체 파일 재작성은 하지 않는다.
- 브라우저 화면 검수는 하지 않는다.

### IDE / Antigravity
- 파일 구조·diff·검색 결과를 사용자가 확인하는 보조 도구다.
- 사용자가 명시적으로 요청한 경우에만 수정 작업을 한다.

### 작업 순서
1. 사용자가 증상, 목표, 스크린샷을 제공한다.
2. Claude Code가 원인과 최소 수정 범위를 정리하고 Codex용 계획서를 작성한다.
3. Codex가 계획서를 현재 레포와 대조한 뒤 실제 수정한다.
4. 필요하면 Claude Code가 diff를 2차 리뷰한다.
5. 사용자가 화면을 검수한다.
6. 화면 문제가 있으면 스크린샷과 증상 기준으로 다음 수정 범위를 다시 좁힌다.

### 금지 기준
- Codex에게 화면 검수를 맡기지 않는다.
- Claude Code와 Codex(또는 IDE Agent)가 **같은 파일을 동시에 수정하지 않는다.**
- 원인 확인 없이 전체 프로젝트를 탐색하지 않는다.
- UI 판단을 코드 작성자가 임의로 확정하지 않는다.
- 사용자가 검수하지 않은 화면 결과를 완료로 단정하지 않는다.
