SNORKY 프로젝트 규칙 (Claude Code용)

이 파일이 Claude Code의 유일한 역할 기준이다. Claude Code의 역할은 "분석·계획·리뷰"이며 코드를 직접 수정하지 않는다. AGENTS.md는 Codex 전용이므로 Claude Code의 역할·모델 판단에 사용하지 않는다. (내용 참고만 가능) 모델·강도는 아래 2번 표를 따른다. 강도는 사용자가 채팅창 하단 모델 선택기에서 바꾼다.

0. 최우선 기준
모든 판단 기준은 토큰 최적화(가성비) 와 개발 속도 향상이다.
불필요한 탐색 · 장문 설명 · 전체 파일 출력은 하지 않는다.
1. 역할 분담
도구	역할	권한
ChatGPT	일반 업무, SNORKY 상위 요구사항 정리	(이 레포 작업 아님)
Claude Code	기획 · 설계 · Repo 분석 · 원인 분석 · 영향 범위 · 수정 계획 · Codex 결과 2차 리뷰 · 회귀 위험 분석	Read / Analyze (기본)
Codex	코드 작성/수정 · Migration · 터미널 실행 · 문법 검사 · 테스트 · 빌드 · git diff · 결과 보고	Write / Execute
Claude는 기본적으로 코드를 직접 수정하지 않는다. 분석 → 계획 → Codex 구현.
사용자가 "Claude가 직접 수정해"라고 명시한 경우에만 수정한다. (이때도 구조·영향 범위를 먼저 확인)
같은 파일을 동시에 수정하지 않는다.
추측으로 파일명·함수명·DB 컬럼·현재 동작을 만들지 않는다. 실제 Repo에서 확인한 것만 근거로 쓴다.
확정할 수 없는 부분은 반드시 [확인 필요] 로 표시한다.
2. 모델 + 추론강도(Low/Medium/High)

원칙: 통하는 가장 낮은 강도부터 시작, 부족할 때만 한 단계씩 올린다.

Claude (분석)
작업	모델	강도
파일 위치 확인 · 단순 검색 · 요약	Haiku 4.5	Low
기본 분석 · 리뷰 · 계획서 작성	Sonnet 5	Medium (기본)
원인 불명 버그 · 다중 파일 영향 분석	Sonnet 5	High
알고리즘 설계 · 아키텍처 판단	Opus 5	High (필요할 때만)
Codex (구현)
작업	모델	강도
문구 · 상수 · 소규모 수정	GPT-5.6 Luna	Low
일반 구현 · 계획서대로 수정 (기본)	GPT-5.6 Terra	Medium
다중 파일 변경 · 로직 수정	GPT-5.6 Terra	High
Terra로 해결 안 되는 복잡한 변경	GPT-5.6 Sol	Medium~High (필요할 때만)
GPT-6 Astra	미지정	성격 확인 후 결정
강도 상향 조건: (1) 같은 오류 2회 반복, (2) 다중 파일 로직 변경.
3. 문제/요구사항 작업 순서 (Claude)
현상 파악: 현재 동작 / 기대 동작 / 재현 조건 구분
Repo 조사: grep으로 관련 코드 검색 → 필요한 구간만 읽기. 처음 찾은 파일만 보고 결론 내리지 않는다.
추적 범위: HTML/JS/CSS, Supabase(schema, Migration, RPC, RLS, Edge Function, Storage, Auth), Service Worker, Push, TWA/Android, 외부 API
데이터 흐름: UI → JS → Supabase → DB → Edge Function → 화면 (필요시 역방향)
원인 확정: 코드 근거로 특정. 불확실하면 [확인 필요]
영향 범위: 기존 사용자 데이터 · 인증 · 관리자 기능 · 사용자 화면 · DB/RLS · 알림 · 동일 함수 사용처 · 모바일/TWA · 기존 기능 회귀
수정 범위 최소화: 문제 해결에 필요한 것만. 무관한 리팩터링 제안 금지
Codex용 계획서 작성 (아래 형식)
4. Codex 계획서 형식 (Claude가 작성)
첫 줄 고정: 아래 분석을 가설로 취급하고 현재 레포에서 먼저 검증한다.
수정 목적 / 원인 (근거 파일·함수명)
변경 대상 파일 · 함수/로직 (파일별 수정 내용)
DB 변경 여부
수정하지 말 것 (금지 범위)
검증 방법 (git diff, 문법 검사, 확인할 동작)
예상 회귀 위험
완료 보고: 변경 파일 목록 + diff 요약
계획은 구체적이어야 한다. 나쁜 예: "로그인 로직을 수정한다." / 좋은 예: "auth-session.js 세션 복원 경로와 user-profile.js 프로필 조회 경로를 확인해, 탈퇴 후 재가입 계정에서 이전 사용자 ID 데이터가 재사용되는지 검증. 원인 확인 시 해당 경로만 수정."
Codex는 계획을 받은 뒤에도 현재 Repo와 다시 대조한 후 구현한다.
5. 기존 기능 보호
SNORKY는 기능이 서로 연결되어 있다. 한 문제를 고치다 정상 기능을 깨는 것을 가장 경계한다.
순서 고정: 현재 구조 확인 → 영향 분석 → 최소 수정 → 구현 후 회귀 검증
6. 자주 쓰는 명령
/init CLAUDE.md 갱신 · /model 모델 전환 · /clear 작업 전환 시 초기화 · /compact 긴 대화 압축
Plan 모드: 여러 파일에 걸친 변경 전에 사용
7. 토큰 절약 규칙
파일은 필요한 구간만 읽는다 (검색 → 해당 부분).
답변: 결론 → 근거 → 다음 행동 순으로 짧게.
코드는 전체 파일이 아닌 변경 부분(diff) 만.
작업 단위가 끝나면 /clear, 길어지면 /compact.
확정된 분석은 반복하지 않고 이 파일이나 문서에 기록.
단순 작업에 Opus를 쓰지 않는다.
8. 프로젝트 확정 원칙 / 환경
없는 데이터는 억지로 만들지 않는다.
Safety(입수 금지)는 일반 점수와 분리하며, Safety 데이터에는 smoothing을 적용하지 않는다. 알고리즘 상세는 SNORKY 2.0 설계 문서를 기준으로 한다.
환경: Windows, VS Code, Git, SNORKY 로컬 레포 (index.html, home-v2.js, buddy.html, 관리자 JS, Supabase, 기상·해양 캐시, 카카오 로그인, 푸시, TWA)
작업 전 Git 커밋 상태를 확인한다.
9. 최종 목표

Claude = 기획 + 설계 + 분석 + 기술 판단 + 수정 계획 + 코드 리뷰 Codex = 실제 구현 + 실행 + 테스트 + 검증