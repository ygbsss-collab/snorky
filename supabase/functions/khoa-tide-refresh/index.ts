import{fetchKhoaXml,finite,khoaField,kstInstant}from"../_shared/khoa-client.ts";
import{authorizeRefresh,concurrent,cors,failure,json,mappingKeys,serviceClient}from"../_shared/marine-runtime.ts";
const ENDPOINT="https://apis.data.go.kr/1192136/tideFcstTime/GetTideFcstTimeApiService";
const day=(offset=0)=>new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Seoul",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(Date.now()+offset*86400000)).replace(/-/g,"");
Deno.serve(async request=>{if(request.method==="OPTIONS")return new Response(null,{status:204,headers:cors});if(request.method!=="POST")return json({status:"ERROR",code:"METHOD_NOT_ALLOWED"},405);if(!authorizeRefresh(request))return json({status:"ERROR",code:"UNAUTHORIZED"},401);try{const client=serviceClient(),stations=await mappingKeys(client,"khoa_tide_station"),jobs=stations.flatMap(station=>Array.from({length:6},(_,offset)=>({station,date:day(offset)}))),settled=await concurrent(jobs,3,async job=>{const result=await fetchKhoaXml(ENDPOINT,{pageNo:"1",numOfRows:"300",type:"xml",obsCode:job.station,reqDate:job.date,min:"60"});const rows=result.items.map(item=>{const raw=khoaField(item,"predDate","tideDate"),forecastAt=raw&&/\d{12,14}/.test(raw.replace(/\D/g,""))?kstInstant(raw.replace(/\D/g,"").slice(0,8),raw.replace(/\D/g,"").slice(8)):kstInstant(job.date,khoaField(item,"predHms","fcstHms")),tideLevel=finite(khoaField(item,"tideLevel","predValue","tideH"));return forecastAt&&tideLevel!==null?{forecastAt,tideLevel,item}:null}).filter(Boolean)as any[];if(!rows.length)throw Object.assign(new Error("KHOA tide response contained no valid items"),{code:"EMPTY_RESPONSE"});const issuedAt=new Date().toISOString(),payload=rows.map(row=>({source:"KHOA_TIDE_FORECAST",station_code:job.station,issued_at:issuedAt,forecast_at:row.forecastAt,payload:row.item,normalized_data:{tideLevel:row.tideLevel},fetched_at:issuedAt,updated_at:issuedAt,status:"fresh",stale:false,response_bytes:result.bytes,item_count:rows.length}));const{error}=await client.from("khoa_tide_forecast_cache").upsert(payload,{onConflict:"source,station_code,issued_at,forecast_at"});if(error)throw error;return{key:`${job.station}:${job.date}`,items:rows.length}});return json({status:"READY",stations:stations.length,jobs:jobs.length,success:settled.filter(x=>x.status==="fulfilled").length,failed:settled.filter(x=>x.status==="rejected").length})}catch(error){return json({status:"ERROR",...failure(error)},502)}});
