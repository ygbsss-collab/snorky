export const MARINE_HOURLY_FIELDS=["wave_height","wave_direction","wave_period","swell_wave_height","swell_wave_direction","swell_wave_period","ocean_current_velocity","ocean_current_direction","sea_surface_temperature","sea_level_height_msl"] as const;
export type MarineRow={forecastAt:string;wave_height:number|null;wave_direction:number|null;wave_period:number|null;swell_wave_height:number|null;swell_wave_direction:number|null;sea_surface_temperature:number|null;ocean_current_velocity:number|null;ocean_current_direction:number|null;sea_level_height_msl:number|null};
const finite=(value:unknown)=>value===null||value===undefined||value===""?null:Number.isFinite(Number(value))?Number(value):null;
export const coordinateKey=(latitude:number,longitude:number)=>`${latitude.toFixed(4)}:${longitude.toFixed(4)}`;
export function marineEndpoint(){return(Deno.env.get("OPEN_METEO_MARINE_BASE_URL")||"https://marine-api.open-meteo.com/v1/marine").replace(/\/$/,"")}
export function marineUrl(latitude:number|number[],longitude:number|number[]){const url=new URL(marineEndpoint());url.searchParams.set("latitude",Array.isArray(latitude)?latitude.join(","):String(latitude));url.searchParams.set("longitude",Array.isArray(longitude)?longitude.join(","):String(longitude));url.searchParams.set("hourly",MARINE_HOURLY_FIELDS.join(","));url.searchParams.set("velocity_unit","ms");url.searchParams.set("timezone","Asia/Seoul");url.searchParams.set("past_days","2");url.searchParams.set("forecast_days","7");const key=Deno.env.get("OPEN_METEO_API_KEY");if(key)url.searchParams.set("apikey",key);return url}
export function normalizeMarine(payload:any):MarineRow[]{const hourly=payload?.hourly,times=Array.isArray(hourly?.time)?hourly.time:[];return times.map((forecastAt:string,index:number)=>({forecastAt,wave_height:finite(hourly.wave_height?.[index]),wave_direction:finite(hourly.wave_direction?.[index]),wave_period:finite(hourly.wave_period?.[index]),swell_wave_height:finite(hourly.swell_wave_height?.[index]),swell_wave_direction:finite(hourly.swell_wave_direction?.[index]),swell_wave_period:finite(hourly.swell_wave_period?.[index]),ocean_current_velocity:finite(hourly.ocean_current_velocity?.[index]),ocean_current_direction:finite(hourly.ocean_current_direction?.[index]),sea_surface_temperature:finite(hourly.sea_surface_temperature?.[index]),sea_level_height_msl:finite(hourly.sea_level_height_msl?.[index])})).filter((row:MarineRow)=>Object.entries(row).some(([key,value])=>key!=="forecastAt"&&value!==null))}
